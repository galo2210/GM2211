// Versión de build. Se actualiza A MANO en cada entrega (SISTEMA.md §8.6).
// Todo debug de "no se ven los cambios" empieza comparando esto con lo deployado.
const VERSION = '0.8.0';
const FASE = 'F6 · Plata';
const BUILD_FECHA = '2026-07-13';
